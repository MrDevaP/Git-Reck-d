<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="commrdevapgit_reck_d.httpsgithub.buzztracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginScreen" />
        <activity android:name=".FirstApplicationScreen" />
        <activity android:name=".RegistrationScreen" />
        <activity
            android:name=".LocationsScreen"
            android:label="@string/title_activity_locations_screen" />
        <activity
            android:name=".LocationDetailsScreen"
            android:label="@string/title_activity_location_details_screen" />
        <activity android:name=".DonationsScreen" />
        <activity android:name=".DonationDetailsScreen" />
        <activity android:name=".AddDonation"></activity>
    </application>

</manifest>